<UserControl x:Class="SummonersVault.App.Controls.SearchableFilterBox"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="40"
             Focusable="False">
    <Grid>
        <Border x:Name="FieldBorder"
                Height="40"
                Background="{StaticResource SurfaceMutedBrush}"
                CornerRadius="10"
                SnapsToDevicePixels="True">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=Input}" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource HighlightBrush}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <TextBox x:Name="Input"
                         Text="{Binding Text, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="{Binding ToolTip, ElementName=Root}"
                         Background="{StaticResource TransparentBrush}"
                         BorderThickness="0"
                         Padding="12,8,38,8"
                         Height="40"
                         VerticalContentAlignment="Center"
                         PreviewKeyDown="Input_PreviewKeyDown">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <ToggleButton x:Name="DropDownToggle"
                              Width="38"
                              HorizontalAlignment="Right"
                              Background="{StaticResource TransparentBrush}"
                              BorderThickness="0"
                              Focusable="False"
                              IsTabStop="False"
                              Click="DropDownToggle_Click">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border Background="{StaticResource TransparentBrush}" CornerRadius="0,10,10,0">
                                <Path x:Name="Chevron"
                                      Data="M 1 1 L 5 5 L 9 1"
                                      Stroke="{StaticResource MutedForegroundBrush}"
                                      StrokeThickness="1.5"
                                      RenderTransformOrigin="0.5,0.5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chevron" Property="Stroke" Value="{StaticResource HighlightBrush}"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Chevron" Property="RenderTransform">
                                        <Setter.Value><RotateTransform Angle="180"/></Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>
        </Border>

        <Popup x:Name="SuggestionPopup"
               Placement="Bottom"
               PlacementTarget="{Binding ElementName=FieldBorder}"
               StaysOpen="True"
               AllowsTransparency="True"
               Closed="SuggestionPopup_Closed">
            <Border Width="{Binding ActualWidth, ElementName=Root}"
                    MaxHeight="280"
                    Margin="0,4,0,0"
                    Padding="4"
                    Background="{StaticResource SurfaceRaisedBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="10">
                <ListBox x:Name="SuggestionList"
                         Background="{StaticResource TransparentBrush}"
                         BorderThickness="0"
                         IsTabStop="False"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MouseLeftButtonUp="SuggestionList_MouseLeftButtonUp">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="{StaticResource TransparentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                            <Setter Property="Padding" Value="12,9"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="SuggestionBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{StaticResource TransparentBrush}"
                                                BorderThickness="1"
                                                CornerRadius="7"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="SuggestionBorder" Property="Background" Value="{StaticResource BorderSubtleBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="SuggestionBorder" Property="Background" Value="{StaticResource SurfaceMutedBrush}"/>
                                                <Setter TargetName="SuggestionBorder" Property="BorderBrush" Value="{StaticResource HighlightBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Segments}" Focusable="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Text}" TextWrapping="NoWrap">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMatch}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource HighlightBrush}"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
